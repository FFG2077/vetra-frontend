services:
  frontend:
    build: .
    container_name: vetra-frontend
    restart: unless-stopped

    ports:
      - "5173:5173"
    
    environment:
      CHOKIDAR_USEPOLLING: "true"

    volumes:
      - .:/app
      - /app/node_modules

    command: yarn dev --host
